################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# runtime/src/iree/hal/drivers/local_sync/BUILD.bazel                          #
#                                                                              #
# Use iree_cmake_extra_content from iree/build_defs.oss.bzl to add arbitrary   #
# CMake-only content.                                                          #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

iree_cc_library(
  NAME
    accel_driver
  HDRS
    "accel_device.h"
    "accel_driver.h"
  SRCS
    "accel_device.c"
    "accel_command_buffer.c"
    "accel_event.c"
    "accel_executable.c"
    "accel_driver.c"
    "accel_executable_loader.c"
    "accel_semaphore.c"
    "/home/jueun/jux/project/iree/tools/prof/micro_profiler.c"
    "/home/jueun/jux/project/iree/tools/prof/micro_profiler.h"
  DEPS
    iree::base
    iree::hal
    iree::hal::local
    benchmark
  PUBLIC
)

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###


# The accelerator simulation library (libVtop.so) is loaded dynamically at
# runtime via `dlopen` in `accel_device.c`, so we don't link it here.
# target_link_libraries(iree_hal_drivers_accel_accel_driver PUBLIC Vtop)
